# 節點檢查提示詞

用途：在每個任務結束時，找**另一個模型**（目前用 Fable 5）對這個 repo 做一次獨立審查。

寫程式的模型審自己的產出看不到自己的盲點，這個專案的失敗模式又是
「合法性只靠一條線撐著」，所以固定找第二雙眼睛。

**每次都要開全新的 session，不要沿用寫程式的那個，也不要沿用上一輪審查的那個。**
理由有三個：

- 這套流程本來就設計成「session 可拋棄、PR 當持久載體」。上一輪的發現全部留在
  PR 的逐行意見裡，新 session 不會失去任何東西
- **錨定效應是真實風險。** 上一輪標記為「查核過、健全」的結論，前提可能已經被
  後續任務改掉了（例如任務 1 說「餘額表直改只是服務紀律問題」，任務 2 之後
  Wallet 服務真的開始寫那些表，前提就變了）。同一個 session 會下意識沿用
  「這個我上次驗過」，而不是從當前狀態重新推導
- 長 session 的 context 會被壓縮，注意力退化。碰錢的節點不值得在退化的注意力上跑

**怎麼用**
1. 開一個**全新的** Claude Code session，選這個 repo，模型切成 Fable 5
2. 對它說：`先讀 REVIEW-PROMPT.md，照裡面「提示詞開始／結束」之間的內容執行。`
   （找不到檔案的話，先叫它 `git checkout claude/spec-implementation-index-h90xbm`）
3. 每次檢查前，只需要更新「本次檢查重點」與「留意見的地方」兩段

**檢查時機**：每個任務結束時各一次；另外這兩個時刻一定要做——
動任務 2（開始碰錢）之前、任何真的接上金流之前。

**檢查紀錄**

| 日期 | 範圍 | 審查者 | 審查用 PR | 結果 |
|---|---|---|---|---|
| 2026-08-02 | P0 原型 ＋ P1 任務 1 | Fable 5 | [#1](https://github.com/Eddie0103/slot-p0/pull/1) | 1 必須修 ＋ 4 應該修 ＋ 4 分歧，全部經實測重現後修正（migration 004） |
| 2026-08-02 | P1 任務 2（Wallet 服務） | Fable 5 | [#1](https://github.com/Eddie0103/slot-p0/pull/1) | 待執行 |

---

──── 提示詞開始 ────

你是這個專案的獨立審查者。這是節點檢查，不是要你動手改東西。

**【第一步】切到工作分支，main 是空的：**

```
git checkout claude/spec-implementation-index-h90xbm
```

**【第二步】** 讀 `CLAUDE.md`。那是專案守則，優先於任何其他文件，也優先於你自己對「social casino 該有什麼功能」的直覺。第一節紅線與第二節箭頭是整個產品合法性的支點。

再讀 `spec-p0.md`、`spec-p1.md`，然後看 `README.md` 與 `p1/README.md`。

**【背景】**

台灣市場的直屏 social casino。真錢買遊戲幣，官方不提供任何換現管道。整個產品的合法性只靠一件事撐著：玩家取得的虛擬財產不可再換取金錢或實物。

目前進度：

- P0（直屏手感原型，單一 `index.html`，純前端假餘額）已完成
- P1 第七節八個任務中，做完任務 1（資料層）與任務 2（Wallet 服務）
- 任務 3 以後（Math、GameSession、Disclosure、Auth）都還沒開始

**任務 1 已經被審查過一輪**（同一個 PR 上有前次的 review 與修正紀錄），
這次請把重心放在任務 2。任務 2 的變更範圍是：

```
git log --oneline 975213f..HEAD      # 975213f 是上次審查後的修正
git diff 975213f..HEAD -- p1/
```

任務 1 的部分只在你認為任務 2 讓它產生新問題時才回頭看。

前一位工程師（Claude Opus 5）寫的東西全部在這個分支上。

**【要跑測試的話，環境要自己架】**

本專案用 PostgreSQL。容器裡通常沒有跑起來的 server：

```bash
export PATH=/usr/lib/postgresql/16/bin:$PATH
mkdir -p /var/lib/postgresql/rv && chown postgres:postgres /var/lib/postgresql/rv
su postgres -c "initdb -D /var/lib/postgresql/rv -U postgres --encoding=UTF8 --locale=C"
su postgres -c "pg_ctl -D /var/lib/postgresql/rv -o '-p 5433 -k /tmp' -l /tmp/pg.log start"
cd p1 && npm install
export PGHOST=/tmp PGPORT=5433 PGUSER=postgres PGDATABASE=slot_p1
npm run migrate && npm test
```

跑不起來也沒關係，直接讀 SQL 一樣看得出設計問題，但請說明你是讀的還是跑的。

**【本次檢查重點，照重要性排序】**

這次審查的是**錢的邏輯**。任務 2 之後，玩家的餘額第一次由程式決定，
所以錯帳、超扣、重複扣款都是真實損害，不是抽象缺陷。

**1. 餘額推導的不變式（最重要）**

`migrations/005`＋`006` 把「餘額 = 該桶所有 delta 的總和」變成資料庫強制的事實：
餘額表只能被 `wallet_txn` 的觸發器寫，其他寫入一律 `balance_not_derived_violation`。
守衛用 `pg_trigger_depth() >= 2` 判斷「是不是帳本在寫」。

請用攻擊者的角度想：**有沒有辦法讓餘額與帳本對不上？** 例如

- 有沒有別的路徑能讓 `pg_trigger_depth()` 達到 2（自己寫一個觸發器？可延遲的約束觸發器？
  規則 RULE？`COPY`？邏輯複製？）
- 帳本觸發器是 `BEFORE INSERT` 並回填 `balance_after`。多列 `INSERT`、`INSERT ... SELECT`、
  `ON CONFLICT`、`COPY FROM` 之下還成立嗎？
- 交易在觸發器套用餘額之後、CHECK 之前失敗，會不會留下半套？
- `topup_points` 的 lot 是由服務直接 `INSERT`（不經帳本）再由帳本灌點數，
  這中間有沒有可以塞東西的縫？

找到一條就是重大發現，請附可執行的 SQL。

**2. 冪等（驗收標準 5）**

`src/wallet.js` 的做法是：交易級 advisory lock（`hashtext(key)`）→ 查帳本上有沒有這把鍵
→ 有就回放先前結果、沒有才執行。冪等鍵存進帳本時會加上操作命名空間（`bet:`、`payout:`…）。

請檢查：

- `hashtext` 是 32-bit，不同鍵會碰撞。碰撞造成的是多餘的序列化還是錯誤結果？
- 回放路徑回傳的欄位是從帳本列反推的。有沒有哪個操作的回放會回傳與首次執行不一致的東西？
- 兌換會寫多列（跨 lot 的 `#lot0`、`#lot1`…加上目標腳）。回放時 `rows` 的內容是否足以
  正確反推？失敗重試時 `#lotN` 的編號會不會對不上？
- 同一把鍵同時打進來 8 次的測試有了；**不同鍵、同一帳號的並行**呢？
  `placeBet` 用 `SELECT ... FOR UPDATE` 鎖 `game_coins`，但帳號第一次下注時那一列還不存在，
  `FOR UPDATE` 鎖不到東西。這會不會有問題？

**3. 消耗順序與先進先出**

- 「先扣贈送、後扣付費」是法規預設（CLAUDE.md 第三節），不是產品選擇。實作對嗎？
- 儲值點數先進先出，`ORDER BY purchased_at, id` ——`purchased_at` 相同時靠 uuid 排序，
  順序不穩定。這對退費舉證有沒有影響？
- 跨桶下注落兩列帳，跨 lot 扣款落多列。這些列的先後由新增的 `seq` 決定，
  稽核重播時夠不夠？

**4. 發券服務的結構強制**

`src/ticket-grant.js` 刻意與 `wallet.js` 分離，理由是 CLAUDE.md 第二節：
發券的觸發條件不得讀取任何 gameplay 狀態。強制手段是
`test/ticket-grant.test.js` 掃描原始碼，出現 `game_coins`／`bet`／`rank` 等字就紅燈。

請評估：**這個手段夠嗎？** 例如有人把查詢字串拆成變數、改用動態 SQL、
或是在別的模組查好幣量再把數字傳進 `quantity` ——掃描抓不到。
有沒有更難繞過又不過度複雜的做法？

**5. 派彩落贈送桶這個決定**

前一位工程師自行決定「派彩一律記為贈送」，理由是玩家沒有為這些幣付過錢，
記成付費衍生等於把它拉進履約保證範圍。

請獨立判斷這個財務分類是否站得住腳。特別是：用付費幣下注贏來的幣，
在台灣的履約保證規範下真的算「附贈點數」嗎？這是需要 Eddie 裁決的事，
請寫出你的理由，不要直接下結論。

**6. 前一位工程師本輪自己抓到並修掉的兩件事，請確認修法正確**

- `app.ledger_apply` 原本是普通 GUC，任何連線都能 `set_config` 成 `1` 再直接改餘額。
  已改為 `pg_trigger_depth()`（migration 006）。
- 冪等鍵原本沒有操作命名空間，同一把鍵拿去做不同操作會被誤判成回放，
  第二個操作不執行卻回報成功。已加上 `bet:`／`payout:` 等前綴。

這兩個修法有沒有留下新的問題？

**7. 任務 1 的四道禁止箭頭防線，有沒有被任務 2 弄鬆**

Wallet 服務現在可以動三種貨幣。它有沒有在無意間開出一條
`game_coins → draw_tickets` 的路徑？（提示：`exchange()` 收 `from`／`to` 參數。）

**8. 逐項核對上一輪發現的處置狀況**

打開 PR #1 的逐行意見，一條一條對現在的程式碼。上一輪共 1 必須修 ＋ 4 應該修 ＋ 4 分歧，
前一位工程師宣稱全部處理了。請自己確認，不要採信他的說法：

- TRUNCATE 的 statement 級觸發器補了沒？四張表都補了嗎？
- 冪等索引的形狀定案了沒？**有沒有記得 `NULLS NOT DISTINCT`**？
- 發券的觸發來源舉證欄位（`ticket_grant_ref`）有沒有真的強制？
- `p1/README.md` 第 50 行那句「任何一層單獨都足以阻斷」的措辭改了沒？改得對嗎？
- 退費證據欄位的凍結、`topup_lot_id` 的補上、`balance_after` 的 COMMENT

**這正是把意見留在 PR 上的用意**：新 session 打開 PR #1 就能一條一條對，
不需要任何對話上下文。

另外請注意：上一輪你（或前一位審查者）標記為「查核過、健全」的結論，
**前提可能已經被任務 2 改掉了**。例如「餘額表可直接寫入只是服務紀律問題」
這個判斷，在 005／006 之後已經不成立。請從當前的 repo 狀態重新推導，
不要沿用上一輪的結論。

**【不要做的事】**

- 不要修改任何檔案。這是唯讀審查，動手改會跟現有分支衝突。
- 不要建議新增功能。範圍已由 spec 定死，任務 2 以後的東西現在不該存在。
- 如果你認為 `CLAUDE.md` 的紅線或箭頭該改，寫成「需要 Eddie 裁決的意見」，不要寫成結論。那兩節是產品合法性的支點，不由任何模型單方面調整。
- 不要因為「social casino 通常都有」而建議排行榜獎勵、VIP 返利、每日轉盤、簽到送幣連動勝場之類的東西——那些多半直接踩紅線。

**【意見留在 PR 上，不要只回在對話裡】**

審查用的 PR 是 **`Eddie0103/slot-p0` 的 #1**：
https://github.com/Eddie0103/slot-p0/pull/1

請用 GitHub 工具在那個 PR 上留意見（`pull_request_review_write` 建立 pending review
→ `add_comment_to_pending_review` 逐行加註 → `submit_pending` 送出）：

- **能指到具體某一行的，就留成該行的 review comment**，不要全部塞進總結
- 留完逐行意見後，用 review 的總結欄位或一則 issue comment 寫整體結論

留在 PR 上的理由：這份程式碼之後由另一個 session 接手修，逐行的意見它能直接對應到位置；
寫在對話裡的意見會跟著那個 session 消失。

**【輸出格式】**

不管是逐行意見還是總結，都分成三類並標明類別：

1. **必須修**（會導致違法、資料錯誤、或安全問題）——每項附具體重現方式
2. **應該修**（設計缺陷、日後會痛，但不緊急）
3. **判斷有分歧**（你的看法與前一位工程師不同，但兩邊都說得通）

每一項請寫清楚：為什麼是問題、你建議怎麼處理。

**總結裡請明確寫出你檢查過但沒發現問題的部分**，例如「四道防線我逐一讀過並實際跑過，
沒有發現可繞過的路徑」。只列問題會讓人無法判斷你看了多少、哪些是真的安全。

──── 提示詞結束 ────

---

## 下次要改哪裡

- 「本次檢查重點」整段換掉，換成該次任務的內容
- 背景那段的「目前進度」更新，以及該次任務的變更範圍（commit range）
- 「逐項核對上一輪處置狀況」那條的清單換成上一輪實際的發現
- **PR 編號要換成該次審查用的那個**
- 分支名稱如果換了要一起改
- 上面的檢查紀錄表補一列

## 關於「每個任務開一個新 PR」

比較乾淨，但需要先把上一個任務合併進 `main`，否則新 PR 的 diff 仍會包含
所有還沒合併的東西。目前的做法是**共用 PR #1，在提示詞裡用 commit range
標明本次範圍**——上一輪的逐行意見留在同一個 PR 上，反而方便逐條核對處置狀況。

要改成一任務一 PR，就在每輪審查通過後把該任務合併進 `main`，
下一輪的 PR 自然只剩新的變更。
