# P0 Spec — 直屏老虎機手感原型

## 唯一目的

回答一個問題：**單手直屏玩老虎機，感覺是不是真的比橫屏好？**

不是做產品，不是做 demo 給人看。這份原型的產出是**一組手感參數數值**。

---

## 交付物

單一 `index.html`。HTML / CSS / JS 全部內嵌，零外部依賴、零建置步驟、零 npm。雙擊就能開。

---

## 硬性非目標（做了就是錯）

- 沒有後端、沒有 API、沒有帳號、沒有登入
- 沒有錢包、沒有儲值、沒有 IAP。餘額就是一個 JS 變數
- 沒有法遵層（分級標示、機率公告、退費流程）—— 那是 P1
- **沒有美術資產。** 符號一律用純色圓角矩形 + 單一 Unicode 字元或 emoji
- 沒有框架、沒有 build tool
- 不做多台機、不做大廳、不做設定頁、不做背景音樂

---

## 版面

直屏三帶，固定不變：

```
┌─────────────────────┐
│ 頂帶 ~12dvh          │ 餘額 + 面板開關
├─────────────────────┤
│                     │
│ 中帶（剩餘高度）      │ 盤面
│                     │
├─────────────────────┤
│ 底帶 ~28dvh          │ 下注 −/＋、SPIN、AUTO
└─────────────────────┘
```

**硬性要求**
- SPIN 鍵圓心距螢幕底部 ≤ 15dvh，水平置中，直徑 ≥ 22vw
- 下注 −/＋ 與 AUTO 全部落在底帶內
- 頂帶不可觸發任何遊戲操作（避免誤觸，也逼自己驗證拇指區假設）
- 用 `100dvh` 不要用 `100vh`（行動瀏覽器網址列會吃掉 vh）
- `overscroll-behavior: none` — 禁止整頁捲動與下拉刷新
- `touch-action: manipulation` — 消除點擊延遲與雙擊縮放
- 偵測到橫向 → 蓋一層全螢幕「請直向使用」遮罩。**不要**做橫屏版面

---

## 盤面與規則

- **4 欄 × 5 列**（直屏原生比例，不要用橫屏標準的 5×3）
- 6 種符號，各自權重可調
- 中獎判定：任一列（橫向）出現 4 個相同符號即中獎。**只做這一種規則**，不要建 payline 系統
- 賠付：符號權重倒數 × 下注額。P0 不驗證數學，簡單即可
- RNG 用 `Math.random()`。這是刻意的：P0 不是產品

---

## 核心功能：手感調校面板

**這是整個 P0 最重要的部分。這個做不好，其他都白做。**

一個可收合面板，每個參數一條 slider，**即時生效**（不需重轉、不需重載頁面）。面板上有一顆「複製目前參數」按鈕，把當前所有參數輸出成 JSON 到剪貼簿。

| 參數 | 說明 | 預設 | 範圍 |
|---|---|---|---|
| `spinUpMs` | 起轉加速時長 | 200 | 0–800 |
| `spinConstMs` | 等速旋轉時長 | 600 | 100–3000 |
| `spinDownMs` | 單欄減速時長 | 400 | 100–1500 |
| `reelStaggerMs` | 相鄰欄停止間隔 | 180 | 0–800 |
| `bounceRatio` | 停止回彈幅度（格高比例） | 0.25 | 0–0.8 |
| `bounceMs` | 回彈時長 | 160 | 0–500 |
| `anticipationOn` | 是否啟用期待感停頓 | true | on/off |
| `anticipationMs` | 差一個就中時的額外停頓 | 500 | 0–2500 |
| `winFlashMs` | 中獎符號單次閃爍時長 | 300 | 100–1000 |
| `winFlashCount` | 閃爍次數 | 3 | 1–8 |
| `winCountUpMs` | 得分數字滾動時長 | 800 | 0–3000 |
| `autoIntervalMs` | AUTO 每次間隔 | 400 | 0–2000 |
| `symbolScale` | 符號佔格子比例 | 0.8 | 0.5–1.0 |
| `reelGapPx` | 欄間距 | 8 | 0–24 |

面板可以是小字、可橫向捲動、可以很醜。**它是工具不是產品。**

---

## 音效

四個：起轉、每欄停止、中獎、AUTO tick。用 Web Audio 合成（`OscillatorNode` + `GainNode`），不要外部音檔。

**必須做**：進入畫面先顯示一個「點擊開始」全螢幕遮罩，在該次點擊的 handler 裡呼叫 `audioContext.resume()`。行動瀏覽器要求使用者手勢才能啟動 Web Audio；少了這步，第一次 spin 會完全沒聲音——而老虎機的手感有一半在聲音上，沒聲音會導致誤判。

面板加一顆靜音開關，方便 A/B 測有聲 vs 無聲。

---

## 記錄

每次 spin 存一筆到記憶體陣列：時間戳、下注額、結果盤面、是否中獎、**當時的完整參數快照**。面板一顆「匯出紀錄」輸出 JSON。

用途：事後回看「我覺得手感最爽的那幾把，參數是什麼」。

---

## 驗收標準

1. 在實體手機上單手（拇指）完成 50 次 spin，全程不需換手或調整握姿
2. 調整任一 slider 後，下一次 spin 立即反映，不需重載頁面
3. 「複製目前參數」貼出來是合法 JSON
4. 從冷啟動到第一次聽到音效，中間只需一次點擊
5. 橫向持機時看到提示遮罩，不會看到破版的遊戲畫面

---

## 測試方式（給 Eddie，不是給 AI 的）

- 手機開網址 → **加到主畫面** → 從主畫面啟動。這樣才沒有瀏覽器 chrome，才是真的全螢幕
- **站著、單手、通勤情境**測。坐在桌前用滑鼠點等於沒測——整個假設就是「橫屏假設雙手持機」
- 找 3–5 人各玩 10 分鐘，只問一個問題：跟你平常玩的比，這個好操作嗎
- **不要在 P0 加任何美術。** 加了就分不清「好玩」是因為直屏還是因為圖好看。這是實驗設計問題

---

## 這階段結束要帶走的東西

**一組參數 JSON。不是這份程式碼。**

前端 RNG 在 P1 必須換成 server 端結算（台灣的機率揭露義務下，前端算的架構無法舉證實際機率等於公告機率）。這份 code 本來就是要丟掉的——不要試著把它長大成產品。
